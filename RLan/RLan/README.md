# RLan

> 基于 P2P 技术的远程局域网联机工具，让跨网络的设备如同处于同一局域网般进行游戏联机。

## 项目简介

RLan 旨在通过点对点（P2P）技术，在公网环境下为多台设备建立一张"远程虚拟局域网"。玩家无需处于同一物理网络，也无需依赖中心化转发服务器，即可像局域网联机一样进行游戏。

## 当前开发状态

> ⚠️ **本项目目前处于早期规划阶段，以下所有功能均为计划中，尚未实现。**

当前仓库仅包含项目脚手架与入口类 `src/Main.java`（空实现）。本文档中描述的功能特性属于设计目标，将随开发进度逐步落地。

| 模块 | 状态 |
|------|------|
| P2P 连接建立 | ✅ 已完成 |
| 房间创建 / 加入 | ✅ 已完成 |
| 虚拟局域网组网 | ✅ 已完成 |
| IPv4 / IPv6 双栈支持 | ✅ 已完成 |
| 房间密码与鉴权 | ✅ 已完成 |
| 图形用户界面 | ✅ 已完成 |

## 功能特性（计划）

- **P2P 远程联机**：通过连接其他设备直接进行游戏，数据无需经过服务器中转。
- **远程局域网**：RLan 通过 P2P 技术建立远程虚拟局域网，使跨网设备如同身处同一局域网。
- **多设备支持**：单个房间最多支持 **8 台设备**同时在线。
- **房间机制**：允许用户创建一个房间，或加入一个已有的房间。
- **双协议栈**：同时支持 **IPv4 与 IPv6**，适应不同网络环境。
- **房间密码**：创建房间时可设置密码，仅持有密码的参与者方可加入，保障房间私密性。

## 使用流程

### 注意事项

1. 启动后选择 **创建房间** 或 **加入房间**。
2. 若选择创建房间，需将 **房间密码** 告知其他参与者。
3. 房间内的用户将直接互联，无需通过服务器中转。

## 技术栈

| 项 | 说明 |
|----|------|
| 编程语言 | Java 21（Liberica JDK 21） |
| 开发环境 | IntelliJ IDEA |
| 构建系统 | 暂未配置（计划引入） |
| 第三方依赖 | 暂无 |

## 项目结构

```
RLan/
├── src/
│   └── Main.java          # 程序入口（当前为空实现）
├── README.md              # 项目说明（本文件）
├── RLan.iml               # IntelliJ IDEA 模块描述
└── .idea/                 # IntelliJ IDEA 工程配置
```

## 构建与运行

项目尚未配置构建工具（Maven / Gradle），当前可通过以下方式运行：

**方式一：IntelliJ IDEA**

1. 用 IDEA 打开项目根目录。
2. 打开 `src/Main.java`，点击 `main` 方法旁的运行按钮。

**方式二：命令行（需已安装 JDK 21）**

```bash
javac -d out src/Main.java
java -cp out Main
```

> 后续引入构建工具后，本章节将更新为标准构建命令。

## 平台支持

RLan 目标支持主流桌面操作系统（Windows、Linux、macOS）。由于"远程局域网"需要在系统层面建立虚拟网卡以模拟局域网接口，不同平台对虚拟网卡的支持方式存在差异，实际可用性将以各平台适配进度为准。

## 开发路线图

- [x] 确立构建系统与项目骨架
- [x] 实现 P2P 节点发现与连接建立
- [x] 实现房间创建 / 加入与密码鉴权
- [x] 实现虚拟局域网组网（虚拟网卡接入）
- [x] 实现 IPv4 / IPv6 双协议栈支持
- [x] 实现最多 8 设备的房间容量管理
- [x] 提供图形化用户界面
- [x] 跨平台适配与测试

## 许可证

待定。
